<?xml version="1.0" encoding="UTF-8"?>
<!--
    LocationMapper的MyBatis映射文件
    功能要求：定义地点相关的SQL语句，包含坐标查询
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redhistory.mapper.LocationMapper">
    
    <select id="selectAllLocations" resultType="com.redhistory.model.Location">
        SELECT * FROM locations ORDER BY name
    </select>
    
    <select id="selectLocationById" resultType="com.redhistory.model.Location">
        SELECT * FROM locations WHERE id = #{id}
    </select>
    
    <select id="selectLocationsByEvent" resultType="com.redhistory.model.Location">
        SELECT l.* FROM locations l
        INNER JOIN events e ON l.id = e.location_id
        WHERE e.id = #{eventId}
    </select>
    
    <!-- 插入地点 -->
    <insert id="insertLocation" parameterType="com.redhistory.model.Location">
        INSERT INTO locations (id, name, province, longitude, latitude, description)
        VALUES (#{id}, #{name}, #{province}, #{longitude}, #{latitude}, #{description})
    </insert>
    
    <!-- 更新地点 -->
    <update id="updateLocation" parameterType="com.redhistory.model.Location">
        UPDATE locations
        SET name = #{name}, province = #{province}, longitude = #{longitude}, latitude = #{latitude}, description = #{description}
        WHERE id = #{id}
    </update>
    
    <!-- 删除地点 -->
    <delete id="deleteLocation" parameterType="java.lang.String">
        DELETE FROM locations WHERE id = #{id}
    </delete>
    
</mapper>

