## 红色历史时间轴 使用指南

本指南帮助你把当前项目串联成一个**可打开并访问的网页应用**，并说明哪些功能已经完成，哪些在页面上会以“开发中 / 未完成”的形式呈现，方便课堂演示或团队成员上手。

---

## 一、运行环境准备

- **操作系统**：Windows / macOS / Linux 均可  
- **必备软件**：
  - **JDK 8+**（建议 11）：用于运行 Spring Boot 后端
  - **Maven 3+**：用于构建后端（也可以直接用 IDE 运行）
  - **MySQL 5.7+ 或 8+**：存储事件、人物、地点、媒体等数据
  - **Node.js 16+、npm 或 pnpm**：用于运行前端（Vue3 + Vite）
  - 可选：**IDE**（IntelliJ IDEA / VS Code / Cursor）方便查看代码

---

## 二、后端启动步骤（Spring Boot）

后端代码位于：`red-history-timeline-backend`

### 1. 创建数据库并 **必须** 初始化表结构和导入数据

这一步是当前“获取事件列表失败”的根本原因——**如果不建表、不导数据，`/api/events` 一定会报错**。

1. 启动本地 MySQL。
2. 创建数据库（名称建议使用与配置一致的 `red_history`，或根据需要自定，但要同步到 `application.yml`）：

```sql
CREATE DATABASE red_history
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

3. 在命令行或图形工具中，切到这个数据库并执行项目提供的 SQL 脚本（路径：`red-history-timeline-backend/scripts/`）：

```bash
# 方式一：在 MySQL 命令行中
mysql -u root -p
mysql> USE red_history;
mysql> SOURCE E:/Red-History-TimeLine/red-history-timeline-backend/scripts/init-db.sql;
mysql> SOURCE E:/Red-History-TimeLine/red-history-timeline-backend/scripts/import-data.sql;
```

   - `init-db.sql`：**创建所有表结构**（`events`、`locations`、`persons`、`media` 等）。  
   - `import-data.sql`：**导入示例历史数据**，否则前端时间轴会是空的。

> 必须执行完上面两个脚本，`/api/events` 才能正常返回数据，否则前端会出现“获取事件列表失败，请稍后重试”的提示。

### 2. 配置数据库连接与（可选）高德 Key

打开后端配置文件：`red-history-timeline-backend/src/main/resources/application.yml`，检查并按本地环境修改：

- **数据库连接信息**（示例）：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/red_history_timeline?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: 你的用户名
    password: 你的密码
```

- （可选）如需在后端调用高德接口，按实际申请的 Key 填写对应配置项。

> 注意：`application.yml` 在开发规范中被标为“核心配置文件”，建议由负责人修改并在团队内同步。

### 3. 通过 IDE 或命令行启动后端

- 使用 IDE（推荐）
  1. 用 IntelliJ IDEA / VS Code 打开 `red-history-timeline-backend`。
  2. 找到启动类 `RedHistoryApplication`。
  3. 右键运行该类，即可启动 Spring Boot。

- 使用 Maven 命令行

在 `red-history-timeline-backend` 目录执行：

```bash
mvn spring-boot:run
```

启动成功后，后端默认监听：

- **API 基础路径**：`http://localhost:8080/api`
  - 例如：`http://localhost:8080/api/events`、`/api/locations`、`/api/media/event/{eventId}` 等。

---

## 三、前端启动步骤（Vue3 + Vite）

前端代码位于：`red-history-timeline-frontend`

### 1. 安装依赖

在项目根目录或前端目录打开终端，进入前端项目：

```bash
cd red-history-timeline-frontend
npm install
```

（也可以使用 `pnpm install` 或 `yarn`，取决于团队约定）

### 2. 配置前端高德地图 Key

1. 找到文件：`red-history-timeline-frontend/public/config/amapKey.js`
2. 按照文件中的注释，将你申请到的 **高德 Web JS API Key** 填入：

```js
// 示例（以实际 key 为准）
window._AMAP_KEY_ = '你的高德 Web JS API Key';
```

> 如果未配置或 Key 无效，地图相关功能（首页全屏地图、详情页地图）可能无法正常显示，但其他页面内容仍可访问。

### 3. 确认接口代理配置

前端通过 Vite 代理访问后端 `/api` 接口，通常在 `vite.config.js` 中已有类似配置（例如将 `/api` 转发到 `http://localhost:8080`）。  
只要后端按上文默认端口启动，前端无需额外修改即可正常访问后端接口。

### 4. 启动前端开发服务器

在 `red-history-timeline-frontend` 目录执行：

```bash
npm run dev
```

终端会输出访问地址，例如：

- `http://localhost:5173/`（以实际端口为准）

在浏览器中打开该地址，即可看到**红色历史时间轴**首页。

---

## 四、如何在浏览器中体验整个应用

### 1. 首页（时间轴 + 事件卡片 + 全屏地图）

进入 `http://localhost:5173/` 后，可以看到：

- 顶部标题：“红色历史时间轴”及副标题。
- 左侧：时间轴 + 事件卡片列表。
  - 时间轴组件会从后端拉取事件列表（`/api/events`）并按时间顺序展示。
  - 每个事件以标题、日期、类别等信息展示，配有事件图片（按 `/assets/images/{事件ID}.jpg` 规则命名）。
  - **点击某条事件或事件卡片**：会跳转到对应的事件详情页 `/detail/:id`。
- 右侧：全屏高德地图。
  - 会调用 `/api/locations` 加载所有事件地点，并通过 `amapUtils` 在地图上添加标记。
  - **点击地图标记**：会根据地点关联的事件 ID，跳转到该事件的详情页。

### 2. 事件详情页（多媒体 + 地图 + 人物 + 资料）

当你从首页点击事件或地图标记进入 `/detail/:id` 时，可以看到：

- 顶部返回按钮：点击返回首页。
- 事件详情主体：标题、时间、背景、内容等文字信息。
- 多媒体播放器：
  - 图片轮播区域（支持上一张/下一张、缩略图、自动播放）。
  - 视频播放区域（若有视频资源）。
  - 音频播放区域（若有红色音乐等音频资源）。
- 地点地图：展示该事件的发生地点，并在地图上以高德标记的形式高亮显示。
- 相关人物模块：展示与该事件相关的历史人物（数据来自 `/api/persons/event/{eventId}`）。
- 相关资料跳转：提供若干外部链接（如文章、文献、视频等），点击后在新窗口打开。

> 前后端都启动、数据库中已经导入数据时，上述模块会完整展示；如果某类数据为空（例如没有媒体或相关人物），对应区域可能展示为空或提示暂无内容。

---

## 五、“开发中 / 未完成”功能在程序中的体现

由于项目部分高级功能尚在开发阶段，本指南建议在使用和演示时按如下方式理解和说明：

- **时间轴高级筛选与搜索**
  - 代码中已经为时间筛选、地点筛选等预留了结构和注释，但当前版本默认只加载全部事件列表。
  - 在讲解或文档中可标注为：**「开发中：未来将支持按时间段 / 地点筛选事件」**。

- **时间轴 → 地图的强联动**
  - 首页的时间轴已经可以点击跳转到详情页，地图也会通过点击标记跳转详情。
  - 但“在时间轴上选择某个事件后，地图自动平移并高亮对应地点”的交互目前只是预留了 `handleEventClick` 与 `locateToCoordinate` 的代码通道，尚未完全打通。
  - 在课堂演示中可说明为：**「开发中：时间轴与地图的双向联动正在完善」**。

- **人物模块的深度展示**
  - 基础人物列表和详情接口已经实现，`RelatedPerson` 组件会展示与事件关联的人物。
  - 更复杂的交互（如人物弹层、从人物反查其参与的所有事件等）目前尚未实现。
  - 可在页面或讲解中标注：**「粗略完成：后续会补充人物详情弹窗和更多关联展示」**。

- **后台管理 / 内容录入界面**
  - 后端已经具备事件、地点、媒体、人物的增删改查接口，但当前前端没有管理后台页面。
  - 如需录入数据，目前需要通过数据库脚本或调用 API 完成。
  - 在产品角度可视为：**「未完成：仅具备展示端，尚无可视化后台管理界面」**。

- **其他产品级功能（登录、权限、多语言等）**
  - 目前项目聚焦在内容展示与地图联动，暂无用户系统与权限控制。
  - 可统一对外说明为：**「未实现：后续如有需要可扩展用户登录、权限、多语言等功能」**。

如需在页面上直观显示“开发中 / 未完成”的提示（例如在筛选区域或人物模块旁加上小标签），可以在相关 Vue 组件中增加简单的标识文字，不影响现有逻辑。

---

## 六、常见问题与排查建议

- **页面可以打开，但内容空白或列表为空**
  - 优先检查后端是否启动成功、数据库是否有数据。
  - 在浏览器控制台（Network）查看 `/api/events`、`/api/locations` 等接口是否返回 200。

- **地图区域不显示 / 一片灰色**
  - 检查 `public/config/amapKey.js` 中的高德 Key 是否正确、是否开启了 Web JS API 权限。
  - 打开浏览器控制台查看是否有高德 JS SDK 相关报错。

- **前端报错“无法访问 /api/xxx”**
  - 检查后端服务是否启动在 `http://localhost:8080`。
  - 检查 `vite.config.js` 中代理配置是否正确指向后端。

---

## 七、课堂演示与团队协作文案建议

在课堂或答辩中，可以按以下顺序简单介绍：

1. **项目目标**：用交互式时间轴 + 多媒体 + 地图联动的方式，展示红色历史事件。
2. **技术栈**：前端 Vue3 + Vite + 高德地图 API，后端 Spring Boot + MyBatis + MySQL。
3. **核心已完成功能**：
   - 首页时间轴 + 事件卡片 + 全屏地图。
   - 事件详情页的图文、多媒体播放与地图定位。
   - 人物、媒体、地点等多实体数据的统一接口与展示。
4. **当前“开发中 / 未完成”功能**：
   - 时间轴高级筛选与地图强联动。
   - 人物模块的深度交互。
   - 后台管理与产品级扩展功能。
5. **后续规划**：可根据 `現狀.md` 中的评估，列出 2–3 个下一步计划作为展望。

这样，即使部分功能仍在完善中，也能向老师和同学清晰展示项目的整体结构、当前完成度与演进方向。



