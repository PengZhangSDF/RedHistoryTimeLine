# 抗日战争时间轴 — 项目说明文档

---

## 一、选题意义与红色教育价值

本项目以 **1931–1945 年抗日战争** 为主线，选取约二十个具有代表性的历史节点（如九一八事变、一二·九运动、卢沟桥事变、南京大屠杀、抗战胜利等），通过时间轴、地图与多媒体相结合的方式，将这段历史系统化、可视化地呈现给用户。

**教育意义**：抗日战争是中国近代史上关系民族存亡的重大事件，也是世界反法西斯战争的重要组成部分。面向青少年与公众的红色教育，不仅需要史实准确，更需要形式生动、易于理解与记忆。本系统将“时间—空间—人物—事件—资料”有机串联：用户既可沿时间轴把握抗战脉络，又可在地图上直观感受事件发生的地理分布，再通过详情页深入阅读背景、经过与影响，并跳转至人民网、新华网、国家档案局、党史网等权威资料，形成“浏览—理解—溯源”的完整学习路径。这种设计有助于培养历史时空观念、家国情怀与理性思辨能力，具有明确的红色教育与爱国主义教育价值。

---

## 二、系统架构与技术选型说明

系统采用 **前后端分离** 架构，便于分工开发与独立部署。

**前端**：基于 **Vue 3** 与 **Vite** 构建单页应用，使用 **Vue Router** 管理路由（首页 `/`、详情页 `/detail/:id`），**Axios** 统一请求后端接口，**Sass** 编写全局与组件样式。地图能力依赖 **高德地图 JavaScript API 2.x**（通过 `@amap/amap-jsapi-loader` 按需加载）。前端按“页面（views）→ 业务组件（components）→ 工具（utils）→ API（api）”分层：首页整合时间轴（TimeAxis）、事件卡片（EventCard）、全屏地图（FullScreenMap）；详情页整合事件详情、多媒体播放器、地图定位、相关人物与相关资料链接。

**后端**：基于 **Spring Boot 3.x** 与 **Java 17**，采用 **MyBatis** 访问 **MySQL** 数据库，提供 REST 风格接口。按“Controller → Service → Mapper → Model”分层，围绕事件（events）、人物（persons）、地点（locations）、媒体（media）、相关资料（materials）五类实体设计接口，并使用统一响应格式（如 code、msg、data、total）与全局异常处理，便于前端统一解析与错误提示。

**技术选型理由**：Vue 3 生态成熟、易于维护与扩展；Spring Boot 与 MyBatis 在高校与中小企业中普及度高，便于协作与二次开发；高德地图在国内访问稳定、文档完善，适合做历史事件地理可视化。

---

## 三、数据来源与处理方式

**历史事件、人物与地点**：核心数据来源于项目内整理的 **JSON 数据**（如 `data/event_list.json`、`data/historical_data.json` 及人物、地点列表等），字段包含事件 ID、标题、日期、类别、简介、详细描述、背景、经过、影响及关联的人物 ID、地点 ID 等。通过脚本或手工整理生成 **SQL 导入脚本**（如 `import-data-from-json.sql`），在 MySQL 中执行后写入 `events`、`persons`、`locations` 及 `event_person` 关联表，并统一使用 **utf8mb4** 编码以避免中文乱码。

**多媒体资源**：事件配图存放于前端 `public/assets/images/`，按事件 ID 命名（如 E001.png）。视频资源中的 B 站链接来源于 `data/vedio.txt`，按事件标题与事件 ID 对应关系整理后，通过 `import-media-bilibili-9.sql` 写入 `media` 表（type 为 video，url 为 B 站页面地址）；前端播放时识别 B 站链接并转换为嵌入播放器地址。

**相关资料（权威链接）**：来自 `data/相关资料.md`，按事件顺序与事件 ID 映射，每条包含标题、URL 及类型（如历史文献、新闻报道、官方网站等），通过 `import-materials-from-md.sql` 写入 `materials` 表，详情页通过接口按事件 ID 拉取并在“相关资料”区域以链接形式展示，新窗口打开，便于用户延伸阅读。

整体上，数据以“结构化 JSON/表格 + SQL 脚本”的方式进入数据库，保证来源可追溯、格式统一，便于后续增补与校验。

---

## 四、交互设计与用户体验思考

**首页**：左侧为竖直时间轴与事件卡片列表，右侧为全屏高德地图。时间轴支持按时间顺序浏览事件，可缩放时间间距（如通过按钮或 Alt + 滚轮）以调节密度；地图上每个事件地点有钉点及钉点上方的缩略图。交互上：**点击地图钉点** → 时间轴平滑滚动至对应事件，便于“从地图到时间”的对照；**点击钉点上方的缩略图** → 直接进入该事件详情页，减少操作步数。事件卡片与时间轴中的事件均可点击进入详情，形成“时间轴—地图—卡片”三种入口的统一体验。

**详情页**：自上而下呈现事件标题、日期、背景、经过、影响等正文内容；随后是多媒体区域（本地图片与 B 站视频等）、地图定位（小地图展示事件发生地）、相关人物列表，以及“相关资料”链接（跳转至权威网站）。返回按钮明确，方便回到首页继续浏览。设计上注重信息分层：核心史实优先，延伸资料与媒体作为补充，兼顾快速浏览与深度阅读两种需求。

**性能与容错**：图片加载失败时使用占位提示，避免空白或错位；接口请求通过 Axios 封装统一处理错误与 loading 状态，提升稳定感与可访问性。

---

## 五、可扩展性建议

**数据源扩展**：  
- 可接入更多权威数据源（如档案馆 API、党史文献库），通过定时任务或管理后台将新数据写入 `events`、`materials` 等表，或增加“数据源”字段便于区分来源。  
- 媒体资源可扩展至更多视频平台（如央视网、学习强国）或音频、纪录片链接，在 `media` 表中用 type 与 url 规范存储，前端播放器根据 URL 规则选择嵌入式播放或新窗口打开。  
- 人物、地点数据可与百科或地理信息服务对接，用于自动补全简介、坐标等，并在后台提供“人工审核”流程，保证红色内容的准确性。

**多端适配**：  
- 当前前端为 PC 端优先，可通过响应式布局（如已有 `media.scss`）进一步优化平板与手机：时间轴与地图在小屏可改为上下布局或 Tab 切换，地图钉点与按钮尺寸适当放大以利于触控。  
- 若需独立移动端体验，可考虑 Vue 3 + Vant 等 UI 库做 H5 版本，或使用同一套后端接口开发小程序（如微信小程序），实现“一套数据、多端展示”。

**功能扩展**：  
- 增加检索与筛选（按时间范围、地点、人物、关键词），后端对 `events` 等表做条件查询与分页。  
- 支持“专题”或“路线”（如“东北抗战”“正面战场”）聚合多事件，通过新表或标签字段实现，前端增加专题入口与列表。  
- 若面向学校或场馆，可增加简单后台：事件/人物/资料的增删改、媒体上传、审核状态等，便于运营方自主维护内容。

---

## 六、小组任务分配

<!-- 此处由小组成员填写任务分工说明，例如：需求分析、前端开发、后端开发、数据整理、测试与文档等。 -->

---

*文档版本：1.0 | 与当前代码结构及功能保持一致*
